// ============================================================
//  core/Utils — крошечные математические помощники.
//  Без зависимостей. Используются всеми слоями.
// ============================================================
export const clamp = (v, a, b) => (v < a ? a : v > b ? b : v);
export const lerp = (a, b, t) => a + (b - a) * t;
