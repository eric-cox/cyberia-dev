// ============================================================
//  Ледяной голем — исполин. В 10 раз больше героя, один на
//  карту (появляется с шансом 1/3 — см. EnemyFactory).
//  Самый сильный → самый низкий голос: рык 60 Гц.
//  Медленно идёт, долго замахивается и бьёт так, что трясётся
//  экран; рычит при виде игрока и перед каждым ударом.
// ============================================================
import { Enemy } from "./Enemy.js";

export const def = {
  type: "golem",
  art: "golem",
  name: "Ледяной голем",
  hp: 700,
  speed: 24,
  aggro: 230,
  range: 40,
  dmg: 40,
  cd: 3.5,
  scale: 4, // сетка 40×40 → 160 px, в 10 раз больше героя
  r: 26,
  h: 40,
  orbs: 3,
  // исполин — утробный рык 60 Гц
  voice: { freq: 60, wave: "sawtooth", v: 0.22, dur: 0.9, every: 5 },
};

export class GolemEnemy extends Enemy {
  // долгий замах — его видно и слышно заранее
  windupTime() {
    return 0.9;
  }
  strikeDuration() {
    return 0.3;
  }
  strikeLunge() {
    return 130;
  }
  strikeReach() {
    return 22;
  }
  // рычит перед каждым ударом (телеграф)
  windupVoice() {
    return true;
  }
}
