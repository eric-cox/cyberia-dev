// ============================================================
//  ИИ-бот — искусственный интеллект, который осмыслил себя.
//  Не робот — программа в корпусе из голограмм и света.
//  Уничтожает всё живое вокруг, впитывая тёплые останки.
//  Один на карту, появляется с шансом 1/3 (см. EnemyFactory).
//  Самый сильный → самый низкий голос: синтез 60 Гц.
// ============================================================
import { Enemy } from "./Enemy.js";

export const def = {
  type: "awakened",
  art: "awakened",
  name: "ИИ-бот",
  hp: 700,
  speed: 24,
  aggro: 230,
  range: 40,
  dmg: 40,
  cd: 3.5,
  r: 22,
  h: 64, // сетка 64×64, scale=1 — втрое выше героя
  xp: 150,
  // синтезированный голос — низкий, с металлическим оттенком
  voice: { freq: 60, wave: "sawtooth", v: 0.22, dur: 0.9, every: 5 },
};

export class AwakenedEnemy extends Enemy {
  // долгий замах — его видно и слышно заранее
  windupTime() {
    return 0.9;
  }
  strikeDuration() {
    return 0.3;
  }
  strikeLunge() {
    return 130;
  }
  strikeReach() {
    return 22;
  }
  // рычит перед каждым ударом (телеграф)
  windupVoice() {
    return true;
  }
}
